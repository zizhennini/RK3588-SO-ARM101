# RK3588 板端依赖
#
# 设计原则：**复用 LeRobot 自己的驱动**，但不装 torch。
#
# LeRobot 的 motors/ robots/ cameras/ 这几个模块**完全不依赖 torch**（已实测确认
# 可正常 import）。所以板端用 --no-deps 装 lerobot，再单独补它运行时真正需要的
# 少量包即可 —— 换来 2000+ 行久经考验的 Feetech 协议实现、标定流程和相机封装，
# 不必自己重写。

# ---- LeRobot 本体（不装它的依赖，避免拉进 torch ~8GB）----
# 安装顺序：
#   pip install --no-deps lerobot
#   pip install -r requirements.txt

# ---- LeRobot 驱动链路的运行时依赖（从 so_follower/robot.py/motors_bus.py 的
#      import 静态分析得出，全部是轻量包）----
pyserial>=3.5          # 串口
scservo_sdk            # Feetech 官方 SDK（LeRobot 的 feetech 驱动依赖它）
deepdiff>=7            # Robot 基类用
draccus>=0.10           # LeRobot 的配置系统（RobotConfig 基于它）
tqdm>=4.66             # 进度条

# ---- 本项目自身需要 ----
numpy>=1.24,<2.0
opencv-python-headless>=4.8
pyyaml>=6.0

# ---- rknnlite ----
# 不要用 pip 装：用板厂/官方提供的 rknn_toolkit_lite2 wheel，
# 版本必须与主机 rknn-toolkit2 匹配（本项目：2.3.2）。
#   pip install rknn_toolkit_lite2-2.3.2-cp310-cp310-linux_aarch64.whl
# 并确认板端 librknnrt.so 版本：
#   strings $(find / -name 'librknnrt.so' 2>/dev/null | head -1) | grep -i version
